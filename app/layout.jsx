export const metadata = { title: 'SiteSignal — KAP', description: 'Weekly letter approvals' };
import './globals.css';
export default function RootLayout({ children }) {
  return (<html lang="en"><body>{children}</body></html>);
}
